# Shagor — Portfolio + Admin Panel

Personal portfolio website with a private admin panel (mini CMS), so content can be edited from the browser without touching any code.

Zero npm dependencies. Only Node.js (v16+) is required.

## Run

```bash
npm start
